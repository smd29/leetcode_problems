class Solution:
    def judgeSquareSum(self, c: int) -> bool:
        ans = int(sqrt(c))
        for a in range(ans+1):
            b = sqrt(c-(a**2))
            if b == int(b):
                return True
        return Falsea
